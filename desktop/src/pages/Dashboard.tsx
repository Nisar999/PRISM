import { EditorWelcome } from '@/components/layout/EditorWelcome';

/** Home route — approved IDE welcome (Figma 434:2), not the old dashboard. */
export function Dashboard() {
  return <EditorWelcome />;
}
